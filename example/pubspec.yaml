name: example
description: A simple package to show toast message on the web ,support flutter web
version: 1.0.0
author: 
homepage: 

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  pq_toast:
    path: ../

